# Allow Thymeleaf templates to be reloaded at dev time
spring.thymeleaf.cache=false
server.tomcat.accesslog.enabled=true
server.tomcat.basedir=target/tomcat

server.port=8081

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

# Persistence
spring.jpa.properties.javax.persistence.validation.mode=none
spring.datasource.url=jdbc:mysql://localhost:3306/demoDB?createDatabaseIfNotExist=true&useSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=*******
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Mail
spring.mail.default-encoding=UTF-8
spring.mail.host=smtp.mail.yahoo.com
spring.mail.username=username@yahoo.com
spring.mail.password=*******
spring.mail.port=587
spring.mail.protocol=smtp
spring.mail.test-connection=false
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# add to VM options for Async context transfers
# -Dspring.security.strategy=MODE_INHERITABLETHREADLOCAL


# mysql does not work with spring username data type
# so create the schema and tables manually

#create schema if not exists armenDB2;
#USE armenDB2;
#create table users(
#username varchar(50) not null primary key,
#password varchar(500) not null,
#enabled boolean not null
#);
#create table authorities (
#username varchar(50) not null,
#authority varchar(50) not null,
#constraint fk_authorities_users foreign key(username) references users(username)
#);
#create unique index ix_auth_username on authorities (username,authority);
